pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'git checkouut'
                git branch: 'main', url: 'https://github.com/Krishvenky08/code.git'
             }
         }
     }
}
