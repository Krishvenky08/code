pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'git checkout'
                git url: 'https://github.com/Krishvenky08/code.git'
             }
         }
     }
}
